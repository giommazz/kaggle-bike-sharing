Todos
2. perform model selection and obtain results with RMSLE
3. add hyperparameter tuning
4. add prophet, arima, sarima
5. when adding SARIMAX, linear models, prophet or neural networks, consider correlation-based pruning as a preprocessing step

- try on fast.api and PyDantic
- create endpoints with: a) everything until training -> save pipeline object; b) inference 
